package probe

// ApplyDefaults fills cfg with the real-world resolvers, baseline hosts and
// status clients. resolver is the value of --resolver.
func ApplyDefaults(cfg *Config, resolver string) error {
	return nil
}
